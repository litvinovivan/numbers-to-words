To run the ConsoleTest.jar file:
1. Open command prompt or a terminal window (e.g. Windows Powershell) in the folder containing ConsoleTest.jar (or navigate to that folder).
2. type the following command and press Enter:
    java -jar ConsoleTest.jar
3. Press Ctrl-C when finished to exit the program.